name: bus2
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Dependency Injection
  get_it: ^7.2.0

  # Http
  http: ^0.13.5

  # Paths
  path: ^1.9.1

  # Storage Directories
  path_provider: ^2.1.5
  
  # SQLite
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0




flutter:
  uses-material-design: true